# Mise à jour du modèle 3D

هذه الحزمة تضيف إلى الموقع عارضًا ثلاثي الأبعاد متجاوبًا للتجميع الميكانيكي، مع التدوير والتكبير والتحريك والزوايا الجاهزة وملء الشاشة.

## الملفات التي يجب رفعها إلى GitHub

1. افتح مجلد `app` في المستودع، ثم ارفع الملفات الثلاثة الموجودة داخل مجلد `app` في هذه الحزمة:
   - `GarnitureViewer.tsx`
   - `page.tsx`
   - `globals.css`
2. عد إلى الصفحة الرئيسية للمستودع وارفع:
   - `package.json`
   - `package-lock.json`
3. افتح مجلد `public` في المستودع وارفع:
   - `garniture.glb`
4. اضغط `Commit changes` بعد كل عملية رفع.

سيعيد Vercel نشر الموقع تلقائيًا. لا ترفع ملف ZIP نفسه إلى المستودع، بل فك ضغطه وارفع الملفات إلى أماكنها المبينة أعلاه.

## ملاحظات

- النموذج محول من ملف STEP الأصلي ومحسن للمتصفح بحجم يقارب 3.5 ميغابايت.
- العارض يعمل بالفأرة واللمس، ويحتوي على زوايا: منظور، أمام، أعلى، وجانب.
- لا تحذف أي ملف آخر من الموقع.
